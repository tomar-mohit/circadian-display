**Preparing modified code output**

I need to modify the output file to include only the code. I should apply the suggested edit to the original code while keeping the comments intact. The key is to replace the org.gradle.java.home line with the suggested version. The user specifically asked for a plain text code block without markdown, so I should provide the raw content directly and ensure that I'm preserving all unchanged lines exactly. I'll add a comment at the top for clarity.**Preparing modified code output**

I need to modify the output file to include only the code. I should apply the suggested edit to the original code while keeping the comments intact. The key is to replace the org.gradle.java.home line with the suggested version. The user specifically asked for a plain text code block without markdown, so I should provide the raw content directly and ensure that I'm preserving all unchanged lines exactly. I'll add a comment at the top for clarity.# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.java.home=C:/Program Files/Android/Android Studio/jbr

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin
kotlin.code.style=official

